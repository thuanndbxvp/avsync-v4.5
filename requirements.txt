# Auto Edit Video — phụ thuộc
#
# Tool chạy PHẦN LỚN bằng thư viện chuẩn Python 3 + FFmpeg (cài riêng ngoài hệ thống).
# Tuy nhiên vẫn CÓ vài gói pip bắt buộc:
#   - requests + cryptography : kiểm tra bản quyền online (license_client.py)
#
# YÊU CẦU MÔI TRƯỜNG:
#   - Python 3.9+      : https://www.python.org/downloads/
#   - FFmpeg (+ffprobe): https://ffmpeg.org/  (Windows: winget install Gyan.FFmpeg)
#
# GIAO DIỆN (GUI):
#   - Tkinter (legacy, app_legacy.py):  đi kèm sẵn trong CPython, KHÔNG cần pip.
#   - PySide6 (mới,   app.py        ):  Phase 1 trở đi. Cài bằng `pip install PySide6`.
#                                     (xem docs/plan/MSEW-pyside6-phase1.md)

# ---- LICENSE (kích hoạt bản quyền, chỉ cần khi LICENSE_ENABLED=True / build bản bán) ----
# Khi đóng gói .exe bằng Nuitka nhớ: --include-package=cryptography
requests
cryptography

# ---- GUI PySide6 (Phase 1+) ----
# (Khi đóng gói .exe bằng Nuitka nhớ: --enable-plugin=pyside6 --include-package=PySide6)
PySide6

PySide6
